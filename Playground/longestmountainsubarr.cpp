#include <bits/stdc++.h>
using namespace std;
int main()
{
    int size;
    cin >> size;
    vector<int> arr(size);
    for (int i = 0; i < arr.size(); i++)
    {
        cin >> arr[i];
    }

    for (int i = 0; i < size - 2; i++)
    {
        if(arr[i+1]>arr[i]&& arr[i+1]>arr[i+2]){
            
        }
    }
}